---
import type { PersonneRecord } from "../utils/type";

interface Props {
  personne: PersonneRecord;
}

const personne = Astro.props.personne;
---

<a href={`/personnes/${personne.id}`}>{personne.prenom} {personne.nom}</a>
